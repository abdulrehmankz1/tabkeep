// This route is never actually navigated to — its tab bar slot is intercepted
// in the tabs layout to open the Add Expense modal instead of switching tabs.
export default function AddPlaceholder() {
  return null;
}
